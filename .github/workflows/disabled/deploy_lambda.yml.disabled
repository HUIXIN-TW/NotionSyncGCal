# Tombstone: this legacy disabled workflow was intentionally neutralized.
#
# It previously encoded unsafe historical deployment behavior and must not be
# restored as runnable workflow YAML.
#
# Current safe flows:
# - Dev deploy: .github/workflows/lambda-ecr-deploy.dev.yml
# - Production image release: .github/workflows/lambda-ecr-release.yml
# - Production Lambda deploy: .github/workflows/lambda-ecr-deploy.prd.yml
#
# Safety rule: master push must never update Lambda.
